@model IEnumerable<DAI.Models.CarCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.НазваКатегорії)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.КатегоріяВодіяNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.НазваКатегорії)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.КатегоріяВодіяNavigation.КодЗапису)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.КодЗапису">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.КодЗапису">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.КодЗапису">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
