@model IEnumerable<DAI.Models.ParticipantsTrafficAccident>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Серія)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.МаркаАвто)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.НомерTrafficAccidentNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.НомерАвтоNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ТипМашиниNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Серія)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.МаркаАвто)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.НомерTrafficAccidentNavigation.НомерTrafficAccident)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.НомерАвтоNavigation.КодАвто)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ТипМашиниNavigation.КодЗапису)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.НомерЗапису">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.НомерЗапису">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.НомерЗапису">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
