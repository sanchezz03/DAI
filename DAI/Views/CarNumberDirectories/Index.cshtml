@model IEnumerable<DAI.Models.CarNumberDirectory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ФормаВластності)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.НомерВільний)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.НомерАвтоNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ФормаВластності)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.НомерВільний)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.НомерАвтоNavigation.КодАвто)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.КодЗапису">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.КодЗапису">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.КодЗапису">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
